//Author: Miles Glover
//Purpose of File:

package EventCalendar;



public class Event {

    /*
     Event(name: String, dateTime: LocalDateTime)  // Constructor;  optional
name: String  // Name of the event.
dateTime: LocalDateTime // the time and date the event starts.
getName(): string // abstract method that returns the name
getDateTime(): LocalDateTime // method that returns the dateTime.
setDateTime(dateTime: LocalDateTime): void // sets the dateTime.
setName(name: String): void // sets the name of the Event.
compareTo(Event e): int  // compares the date of this Event to the incoming event and returns a positive int if this event comes later, a negative int if this event comes before, and 0 if the two have the same dateTime.

     */

}
