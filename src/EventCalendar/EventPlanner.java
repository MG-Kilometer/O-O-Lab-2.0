//Author: Miles Glover
//Purpose of File:

package EventCalendar;

public class EventPlanner {

    /*

A.	Create a new class called EventPlanner EventPlanner will have a main method that creates a JFrame and then adds an EventListPanel.
static addDefaultEvents(events: EventListPanel): void  // adds some default events to the EventListPanel (at least one Deadline and one Meeting), so something shows up even if your add button doesn’t work.

*/

}
